const global = require("./global.json");
const textarea = require("./textarea.json");
const typography = require("./typography.json");

const router = {
  global: global,
  textarea: textarea,
  typography,
  typography,
};

export default router;
